# Everything the build does not need. Keeps the upload small and keeps working
# screenshots and the SQL out of the shipped archive.
.expo/
.expo-verify/
dist/
node_modules/

# Design and working files
*.jpeg
*.jpg
home.png
insight.png
preview-icons.png

# Server-side and tooling, never bundled into the app
supabase/
scripts/
README.md
